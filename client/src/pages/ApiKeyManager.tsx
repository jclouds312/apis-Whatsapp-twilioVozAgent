import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function ApiKeyManager() {
  return (
    <div className="space-y-6">
      <Card>
        <CardHeader>
          <CardTitle>ApiKeyManager</CardTitle>
        </CardHeader>
        <CardContent>
          <p>This is the ApiKeyManager page. Content coming soon.</p>
        </CardContent>
      </Card>
    </div>
  );
}
