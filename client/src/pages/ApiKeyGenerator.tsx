import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function ApiKeyGenerator() {
  return (
    <div className="space-y-6">
      <Card>
        <CardHeader>
          <CardTitle>ApiKeyGenerator</CardTitle>
        </CardHeader>
        <CardContent>
          <p>This is the ApiKeyGenerator page. Content coming soon.</p>
        </CardContent>
      </Card>
    </div>
  );
}
